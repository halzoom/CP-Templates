1. Order of permutation is the minimum number of operation to make
   identity (pi = i) , period(p) = lcm(all cycle length).

2. For an array with distinct elements, the minimum number of swaps needed to sort it is:
    ∑(cycle length−1)